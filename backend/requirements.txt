# OpenFace 3.0 offline gaze backend — Python deps.
# torch/torchvision are installed in the Dockerfile against the CUDA base image,
# so they are intentionally NOT pinned here (avoid pulling the CPU wheels).

fastapi==0.115.*
uvicorn[standard]==0.32.*
python-multipart==0.0.*        # multipart/form-data video upload
numpy==1.26.*
opencv-python-headless==4.10.* # headless = no GUI libs, smaller image
scikit-learn==1.5.*            # Ridge + PolynomialFeatures for calibration mapping
scipy==1.13.*                  # Savitzky-Golay non-causal smoothing (video.py)
pydantic==2.*

# Facial movement + acoustic speech analysis for /facial-speech/process.
# Both run offline inside the existing Docker backend; no recording leaves the
# user's machine during local development.
mediapipe==0.10.14
praat-parselmouth==0.4.4

# OpenFace 3.0 — multitask gaze/AU/landmark/emotion model.
# Achieves 2.56° angular error on MPIIGaze (vs L2CS-Net's 3.92°).
# Weights downloaded separately via `openface download` (HuggingFace nutPace/openface_weights).
# PyPI name is openface-test during the initial release period.
openface-test
